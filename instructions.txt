intergrated search feature->message->message with chatbot(important)-> interview (if we have time)
Update svg icon for FE
test acc: thanh123-> hashed: $2b$10$DTnk.WVhrmkCHWYIWrpb0u6u.KnZ7/0C8sugZgwmNvW7QCAWLsknW
Create two level menu for some section
| Email                                           | Mật khẩu    |
| ----------------------------------------------- | ----------- |
| [user01@example.com](69c3f6ee40fec794dda64a08) | Pass#01mock |
| [user02@example.com](69c3f71240fec794dda64a0b) | Pass#02mock |
| [user03@example.com](69c3f71f40fec794dda64a0e) | Pass#03mock |
| [user04@example.com](69c3f72e40fec794dda64a11) | Pass#04mock |
| [user05@example.com](69c3f73d40fec794dda64a14) | Pass#05mock |
| [user06@example.com](69c3f74b40fec794dda64a17) | Pass#06mock |
| [user07@example.com](69c3f75940fec794dda64a1a) | Pass#07mock |
| [user08@example.com](69c3f76b40fec794dda64a1d) | Pass#08mock |
| [user09@example.com](69c3f78540fec794dda64a20) | Pass#09mock |
| [user10@example.com](69c3f79340fec794dda64a23) | Pass#10mock |
| [user11@example.com](69c3f7b740fec794dda64a26) | Pass#11mock |
| [user12@example.com](69c3f7c740fec794dda64a29) | Pass#12mock |
| [user13@example.com](69c3f7d940fec794dda64a2c) | Pass#13mock |
| [user14@example.com](69c3f7ee40fec794dda64a2f) | Pass#14mock |
| [user15@example.com](69c3f7fe40fec794dda64a32) | Pass#15mock |
| [user16@example.com](69c3f80b40fec794dda64a35) | Pass#16mock |
| [user17@example.com](69c3f81640fec794dda64a38) | Pass#17mock |
| [user18@example.com](69c3f82440fec794dda64a3b) | Pass#18mock |



Luồng nhắn tin AI:
Người dùng chọn nhắn tin với AI->Đặt ChatTarget={null,"AI Assistant",'AI'}
->Open ChatWidget (chưa có gì thì khởi tạo rỗng)->Kiểm tra kiểu nếu là AI thì bắn event sendMessage với isAI bằng true khi gửi tin nhắn đầu tiên->
sau đó gán ConversationId bằng kết quả trả về (lưu vào localStorage với currentUser và gán TTL=900)->sau đó mỗi lần mở ChatWidget sẽ sử dụng key này (cần thêm cơ chế tự động xóa)
->
gemini 3.1 flash live preview cho pv :
đầu vào: nhận file jd công việc và cv, extract text làm systemInstruction cho mô hình
mô hình: sẽ có systeminstruction riêng stream response ưu tiên qua websocket (hoặc nếu không ổn dùng socket.io)
Prompt Engineering:
  Role: Bạn là một nhà tuyển dụng cấp cao
  Context: Nội dung CV + Nội dung JD.
  Rules: "Chỉ hỏi từng câu một", "Đánh giá dựa trên STAR", "Ngắt lời nếu ứng viên lan man".
Thiết lập kết nối Live: FE gửi yêu cầu bắt đầu phiên pv, NodeJS khởi tạo kết nối WS đến điểm cuối
: wss://generativelanguage.googleapis.com/ws/google.ai.generativelanguage.v1alpha.GenerativeService.BidiGenerateContent
Gửi setup message chứa systeminstruction. AI chào hỏi và đưa câu hỏi đầu tiên -> FE dùng Web Audio API để stream âm thanh trực tiếp lên Gemini.
-> Gemini phản hồi bằng luồng audio. Gửi một prompt ẩn gửi một prompt ẩn: "Dựa trên cuộc hội thoại trên, hãy xuất ra file JSON đánh giá các kỹ năng của ứng viên
-> Hiển thị bảng điểm và nhận xét cho người dùng trên ReactJS.
Frontend gửi HTTP POST /api/interview/initiate (kèm CV, JD).
Backend xử lý, khởi tạo phiên, và gửi lời chào AI đầu tiên qua Socket.IO (ai_audio_chunk).
Frontend lắng nghe ai_audio_chunk để phát âm thanh của AI.
Frontend gửi send_user_text_turn (văn bản đã chuyển đổi từ giọng nói của người dùng) qua Socket.IO.
Backend xử lý, gửi phản hồi AI qua Socket.IO (ai_audio_chunk).
Lặp lại bước 3-5 cho đến khi phỏng vấn kết thúc.
Frontend gửi end_interview qua Socket.IO.
Backend xử lý kết thúc, gửi tin nhắn cuối cùng của AI (ai_audio_chunk) và kết quả phân tích (interview_ended).
Lỗi: Sau một tuần không đăng nhập và bị văng, thì có lỗi xảy ra khiến khi truy cập vào trang chủ làm nó tự động chuyển hướng đến trang đăng nhập mà không thể vào trang chủ.

export const runGemini = async (messages, onChunk, userId, systemInstruction) => {
  try {
    const model = genAI.getGenerativeModel({
      model: "gemma-4-31b-it", 
      systemInstruction: systemInstruction, 
      tools: tools 
    });

    // 2. Chuyển đổi lịch sử tin nhắn sang định dạng Gemini (loại bỏ tin nhắn cuối cùng để gửi riêng)
    const chatHistory = messages.slice(0, -1).map(msg => {
  return {
    role: msg.role === "user" ? "user" : "model",
    parts: Array.isArray(msg.parts) 
      ? msg.parts 
      : [{ text: msg.content || "" }]
  };
});

    const chat = model.startChat({
      history: chatHistory
    });

    // 3. Gửi tin nhắn mới nhất
    const latestUserMessageContent = messages[messages.length - 1].content;
    console.log(JSON.stringify(chatHistory, null, 2))
    let result = await chat.sendMessageStream(latestUserMessageContent);

    // Hàm đệ quy hoặc vòng lặp để xử lý phản hồi (bao gồm cả Tool Calling)
    await handleStream(result, chat, onChunk, userId);

  } catch (error) {
    handleError(error, onChunk);
  }
};

/**
 * Hàm phụ trách xử lý Stream và tự động gọi lại nếu Gemini yêu cầu sử dụng Tool
 */
async function handleStream(result, chat, onChunk, userId) {
  let functionCalls = [];
  let fullTextResponse = ""; // Lưu lại để kiểm tra xem có text thật không

  for await (const chunk of result.stream) {
    try {
      const chunkText = chunk.text();
      if (chunkText && chunkText.trim() !== "") { // Chỉ gửi nếu có text thật
        fullTextResponse += chunkText;
        onChunk(chunkText);
      }
    } catch (e) {
      // Chunk này là function call, không có text, bỏ qua lỗi chunk.text()
    }

    const calls = chunk.functionCalls();
    if (calls && calls.length > 0) {
      functionCalls = calls;
    }
  }

  if (functionCalls.length > 0) {
    const toolResponses = [];
    for (const fnCall of functionCalls) {
      const functionOutput = await executeTool(fnCall.name, fnCall.args, userId);
      toolResponses.push({
        functionResponse: {
          name: fnCall.name,
          response: functionOutput
        }
      });
    }

    // QUAN TRỌNG: Gửi trả kết quả tool
    // SDK sẽ tự động khớp toolResponses này với functionCalls trước đó trong bộ nhớ của 'chat'
    const secondResult = await chat.sendMessageStream(toolResponses);
    await handleStream(secondResult, chat, onChunk, userId);
  }
}


  1. Giới thiệu chung
  Chúng tôi đang tìm kiếm những tài năng trẻ, đam mê công nghệ và có tư duy logic tốt để gia nhập đội ngũ phát triển sản phẩm. Tại đây, bạn không chỉ "copy-paste" code, mà sẽ tham gia trực tiếp vào vòng đời phát triển phần mềm (SDLC), từ khâu ý tưởng đến khi triển khai sản phẩm thực tế.
  2. Trách nhiệm công việc (Responsibilities)
      Phát triển tính năng: Hỗ trợ thiết kế, lập trình và duy trì các module tính năng cho ứng dụng web (Frontend & Backend).
      Fix bug & Tối ưu: Tìm kiếm nguyên nhân và xử lý các lỗi kỹ thuật trong quá trình phát triển; tối ưu hóa hiệu suất mã nguồn.
      Nghiên cứu công nghệ: Tìm hiểu và ứng dụng các công nghệ mới để giải quyết các bài toán cụ thể của dự án.
      Làm việc nhóm: Phối hợp chặt chẽ với các Senior Engineer và Designer thông qua quy trình Agile/Scrum.
      Viết tài liệu: Thực hiện viết Unit Test và tài liệu kỹ thuật (Technical Documentation) cho các tính năng mình đảm nhận.
  3. Yêu cầu chuyên môn (Requirements)
      Nền tảng vững chắc: Đang theo học hoặc vừa tốt nghiệp chuyên ngành CNTT, Khoa học máy tính hoặc các ngành tương đương.
      Tư duy lập trình: Hiểu rõ về cấu trúc dữ liệu và giải thuật (Data Structures & Algorithms).
      Ngôn ngữ lập trình: Có kiến thức cơ bản về ít nhất một ngôn ngữ (Javascript, Python, Java hoặc C#).
      Web Cơ bản: Nắm vững HTML5, CSS3 và Javascript (ES6+). Ưu tiên ứng viên biết sử dụng các framework như React, Vue hoặc Node.js.
      Cơ sở dữ liệu: Hiểu về SQL (MySQL, PostgreSQL) hoặc NoSQL (MongoDB).
      Công cụ quản lý: Biết sử dụng Git để quản lý phiên bản mã nguồn.
  4. Kỹ năng mềm & Điểm cộng (Nice to have)
      Có khả năng đọc hiểu tài liệu tiếng Anh chuyên ngành tốt.
      Tư duy giải quyết vấn đề (Problem-solving) chủ động.
      Đã từng có sản phẩm cá nhân hoặc dự án đồ án thực tế trên GitHub.
  5. Quyền lợi
      Môi trường làm việc trẻ trung, sáng tạo, không ngại sai lầm.
      Được dẫn dắt trực tiếp bởi các Mentor giàu kinh nghiệm.
      Hỗ trợ chi phí thực tập và cơ hội trở thành nhân viên chính thức sau kỳ thực tập.


